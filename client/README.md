# AudiVise音视频语音内容理解平台 Web

基于 React、TypeScript 与 Vite 的前端工作台，支持上传已有音频或视频文件，并展示异步解析进度、时间轴字幕、AI 摘要、证据问答和 Agent Trace。

项目不采集麦克风录音。音频使用 `<audio>` 播放器，视频使用 `<video>` 播放器。

```powershell
npm install
npm test
npm run build
npm run dev
```
