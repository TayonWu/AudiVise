# AudiVise音视频语音内容理解平台：Legacy implementation

This directory preserves the original Spring Boot, RocketMQ, and Vue-era backend assets for migration comparison only.

The active AudiVise implementation is:

- `../backend`: FastAPI, Celery, LangGraph, PostgreSQL, MinIO, Qdrant
- `../client`: React and TypeScript
- `../docker-compose.yml`: current local deployment
